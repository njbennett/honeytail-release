[Required Options]
ParserName = json

WriteKey = some-write-key

Dataset = some-dataset

{{range $logFile := .LogFiles}}LogFiles = {{ $logFile }}
{{end}}
[ Application Options ]
# add some BOSH-provided fields to all events
AddFields = bosh.spec.deployment=concourse
